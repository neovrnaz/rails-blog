<%= content_for :title, "Blog Pages" %>

<h1>Blog Pages</h1>

<div class="page-card">
  <% @pages.each do |page| %>
    <p>
      <%= link_to page.title, page %>
    </p>
  <% end %>
</div>

<%= link_to 'New Page', new_page_path %>


